# models/staging/schema.yml
# DQ Tests สำหรับ staging layer — รันด้วย dbt test

version: 2

models:
  - name: stg_market_1
    description: "Cleaned staging data จาก Market 1"
    columns:
      - name: date_created
        tests:
          - not_null
      - name: contacts_n
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: ">= 0"
      - name: new_type
        tests:
          - not_null
      - name: new_market
        tests:
          - not_null

  - name: stg_market_2
    description: "Cleaned staging data จาก Market 2"
    columns:
      - name: date_created
        tests:
          - not_null
      - name: contacts_n
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: ">= 0"
      - name: new_type
        tests:
          - not_null
      - name: new_market
        tests:
          - not_null

  - name: stg_market_3
    description: "Cleaned staging data จาก Market 3"
    columns:
      - name: date_created
        tests:
          - not_null
      - name: contacts_n
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: ">= 0"
      - name: new_type
        tests:
          - not_null
      - name: new_market
        tests:
          - not_null
